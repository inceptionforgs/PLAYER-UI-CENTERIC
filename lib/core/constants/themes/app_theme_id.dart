/// Identifiers for each built-in theme, plus [custom] for the
/// user-picked colour + shade theme (see custom_theme_builder.dart).
enum AppThemeId { walkmanOrange, cyberBlack, silverChrome, custom }

